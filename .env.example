# Copy to .env.local and fill in the real numbers.
# Both are optional — they fall back to the design placeholder "+91 90000 00000".
# NEXT_PUBLIC_ vars are inlined into the client bundle, so keep them non-secret.

NEXT_PUBLIC_PHONE_NUMBER="+91 90000 00000"
NEXT_PUBLIC_WHATSAPP_NUMBER="+91 90000 00000"
